#include <stdio.h>

int main(){

    //char *ptr = "we are best";  
    //{ choose any one of these method to initialize the string}
    char ptr[] = "we are good" ;
    printf("%s",ptr);
    return 0;
}